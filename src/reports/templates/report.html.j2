<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="utf-8" />
  <title>Relatório SRAG — UF {{ uf }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body { font-family: system-ui, -apple-system, "Segoe UI", Roboto, Arial; margin: 24px; color: #111; }
    h1 { margin-bottom: 0.2rem; }
    .sub { color: #666; margin-top: 0; }
    .grid { display: grid; grid-template-columns: repeat(auto-fit,minmax(220px,1fr)); gap: 16px; margin: 16px 0 24px; }
    .card { border: 1px solid #e5e7eb; border-radius: 12px; padding: 16px; }
    .kpi { font-size: 1.8rem; font-weight: 700; margin: 8px 0; }
    .muted { color: #666; font-size: 0.9rem; }
    img { max-width: 100%; height: auto; border: 1px solid #eee; border-radius: 12px; }
    footer { margin-top: 24px; color: #666; font-size: 0.9rem; }
  </style>
</head>
<body>
  <h1>Relatório SRAG — UF {{ uf }}</h1>
  <p class="sub">Gerado em {{ now }}</p>

  <div class="grid">
    <div class="card">
      <div class="muted">Variação de casos (mês atual vs anterior)</div>
      <div class="kpi">
        {% if increase_rate is not none %}
          {{ (increase_rate * 100) | round(1) }}%
        {% else %}N/A{% endif %}
      </div>
    </div>
    <div class="card">
      <div class="muted">Taxa de mortalidade (mês mais recente)</div>
      <div class="kpi">
        {% if mortality_rate is not none %}
          {{ (mortality_rate * 100) | round(2) }}%
        {% else %}N/A{% endif %}
      </div>
    </div>
    <div class="card">
      <div class="muted">Taxa de UTI (mês mais recente)</div>
      <div class="kpi">
        {% if icu_rate is not none %}
          {{ (icu_rate * 100) | round(2) }}%
        {% else %}N/A{% endif %}
      </div>
    </div>
    <div class="card">
      <div class="muted">Taxa de vacinação (proxy nos casos)</div>
      <div class="kpi">
        {% if vaccination_rate is not none %}
          {{ (vaccination_rate * 100) | round(2) }}%
        {% else %}N/A{% endif %}
      </div>
    </div>
  </div>

  <h2>Casos — últimos 30 dias</h2>
  {% if chart_30d %}
    <img src="{{ chart_30d }}" alt="Casos diários (30 dias)">
  {% else %}
    <p class="muted">Sem dados nos últimos 30 dias para {{ uf }}.</p>
  {% endif %}

  <h2>Casos — últimos 12 meses</h2>
  {% if chart_12m %}
    <img src="{{ chart_12m }}" alt="Casos mensais (12 meses)">
  {% else %}
    <p class="muted">Sem dados nos últimos 12 meses para {{ uf }}.</p>
  {% endif %}

  <h2>Contexto de notícias</h2>
  <p>{{ news_summary or "Sem notícias recentes encontradas." }}</p>

  <footer>Fonte: OpenDataSUS (SRAG). Este relatório é gerado automaticamente.</footer>
</body>
</html>
